CREATE TABLE Student(
    Name     VARCHAR(10),
    Age      INT,
    RollNo   INT Primary KEY
);

INSERT INTO Student 
VALUES
('Rupin',19,101),
('Rohan',16,121),
('Goram',18,181);

SELECT * FROM Student;

CREATE TABLE Marks(
    RollNo     INT PRIMARY KEY,
    Subject  VARCHAR(10),
    Marks      INT,
    Grade   CHAR(5)
);

INSERT INTO Marks
VALUES
(101,'Maths',80,'A'),
(121,'Phy',75,'B'),
(181,'Chem',90,'A'),
(191,'Chem',90,'A');

SELECT *FROM Marks;

/*ALTER*/
ALTER TABLE Student      /* ADD COLUMN */
ADD  Email VARCHAR(100);


ALTER TABLE Student          /* MODIFY COLUMN */
MODIFY Name VARCHAR(100);

ALTER TABLE Student                   /* RENAME COLUMN NAME */
RENAME COLUMN Name TO Student_Name;

ALTER TABLE Student
DROP COLUMN Email;    
               /* REMOVE A COLUMN */
SELECT * FROM Student;

/*DELETE FROM Student;*/      /* DELETE ALL ROWS */

DELETE FROM Student          /* DELETE SELECTED ROWS */
WHERE Age > 16;

   

TRUNCATE TABLE Student;     /* REMOVES ALL DATA,THE TABLE REMAINS */
SELECT *FROM Student;

/*ALTER TABLE Student
RENAME TO Students;
SELECT * FROM Students;
*/

/* DML-Manages the actual data inside the database */
/* DDL- TABLE OPERATIONS */
